<%- model_class = MarketSegmentation -%>
<div class="page-header">
  <h1><%=t '.title', :default => MarketSegmentation %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @market_segmentations.each do |market_segmentation| %>
      <tr>
        <td><%= market_segmentation.id %></td>
        <td><%=l market_segmentation.created_at, format: :long %></td>
        <td>
          <%= link_to market_segmentation_path(market_segmentation), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to market_segmentation_path(market_segmentation), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_market_segmentation_path,
            :class => 'btn btn-primary' %>
